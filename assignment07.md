## Q01. 이진 검색 트리에서 균형이 맞지 않을 때의 점근적 검색 시간

- **최악의 경우**: **O(n)**
- **최선의 경우**: **O(log n)**


---

## Q02. AVL 트리 균형 위반 

- 노드 10의 균형 인수: 왼쪽 높이 = 0, 오른쪽 높이 = 2 → **bf = -2 (균형 위반)**

**삽입과 삭제 둘 다 가능하다.**

- **삽입의 경우**: 17 또는 30을 삽입하면 10의 오른쪽 서브트리가 깊어져 불균형 발생
- **삭제의 경우**: 원래 10의 왼쪽에 있던 자식(예: 5)을 삭제하면 불균형 발생

---

## Q03. AVL 트리 균형 위반 (40, 10, 55, 5, 20, 17)

- 노드 40의 균형 인수: 왼쪽 높이 = 3, 오른쪽 높이 = 1 → **bf = 2 (균형 위반)**
- 노드 10의 균형 인수: 왼쪽 높이 = 0, 오른쪽 높이 = 2 → **bf = -2 (균형 위반)**

**삽입과 삭제 둘 다 가능하다.**

- **삽입의 경우**: 17을 삽입하면 10의 오른쪽 서브트리가 깊어져 불균형 발생
- **삭제의 경우**: 원래 55 아래에 자식이 있었는데 삭제하면 오른쪽 서브트리가 낮아져 불균형 발생

---

## Q04. Q03 트리를 AVL 트리로 수선

노드 40 → 10 → 20 → 17 경로이므로 **LR 케이스** (Left-Right)

### Step 1: 불균형 트리 확인

```
        40  (bf=2, 위반)
       /  \
     10    55  (bf=0)
(bf=-2)/  \
     5    20  (bf=1)
          /
         17
```

노드 10이 LR 케이스: 왼쪽 자식(10)의 오른쪽 서브트리(20→17)가 더 깊음

### Step 2: 1차 회전 — 노드 10을 중심으로 좌회전
```
        40  (bf=2, 아직 위반)
       /  \
     20    55
    /  \
  10   17
  /
 5
```

- 20이 10의 자리로 올라옴
- 10은 20의 왼쪽 자식, 17은 20의 오른쪽 자식이었으나 10의 오른쪽으로 이동
- 5는 10의 왼쪽 자식으로 유지

### Step 3: 2차 회전 — 노드 40을 중심으로 우회전
```
        20  (bf=0 ✓)
       /  \
     10    40  (bf=0 ✓)
    /  \     \
   5   17    55  (bf=0 ✓)
```

- 20이 새 루트
- 10은 20의 왼쪽 서브트리, 40은 20의 오른쪽 서브트리
- 모든 노드의 |bf| ≤ 1 → **AVL 조건 만족**

---

## Q05. 한 번의 회전으로 균형이 맞추어지지 않는 경우

| 케이스 | 회전 방법 | 횟수 |
|--------|-----------|------|
| LL | 오른쪽 단순 회전 | 1회 |
| RR | 왼쪽 단순 회전 | 1회 |
| **LR** | **왼쪽 → 오른쪽 이중 회전** | **2회** |
| **RL** | **오른쪽 → 왼쪽 이중 회전** | **2회** |

**LR 케이스와 RL 케이스**는 한 번의 회전으로 균형이 맞추어지지 않으며, 이중 회전이 필요하다.

---

## Q06. "수선 후 AVL 트리의 높이가 반드시 낮아진다" — 맞는 주장인가?

**틀린 주장이다.**

- **삽입 후 수선**: 회전으로 균형을 잡으면 해당 서브트리의 높이가 삽입 전과 같아진다 (낮아지지 않음)
- **삭제 후 수선**: 높이가 낮아질 수도 있지만, 그대로 유지되는 경우도 있다

따라서 수선 후 높이가 **반드시** 낮아진다는 보장은 없다.

---

## Q07. 위로 올라가면서 3회 이상 수선이 필요한 경우 — 가능한가?

**가능하다.**

- **삽입 연산**: 최대 1~2번 회전으로 불균형이 해소되고 전파가 멈추므로 3회 이상 수선은 발생하지 않는다.
- **삭제 연산**: 한 노드를 수선해도 부모 노드에서 다시 균형이 깨질 수 있고, 이것이 연쇄적으로 루트까지 전파될 수 있다. 이론적으로 최대 O(log n)번 수선이 필요할 수 있으므로 3회 이상도 가능하다.

---

## Q08. RB 트리에서 키 7 삽입

### 삽입 전 RB 트리

```
          10(B)
         /     \
       5(R)   13(B)
      /   \       \
    3(B)  6(B)   15(R)
    /        \
  2(R)       8(R)
```

### 삽입 후 최종 RB 트리

```
          10(B)
         /     \
       5(B)   13(B)
      /   \       \
    3(B)  6(B)   15(R)
    /        \
  2(B)       8(B)
             /
            7(R)  ← 새로 삽입
```

> **B** = Black, **R** = Red

- 모든 RB 트리 성질 만족 ✓

---

## Q09. RB 트리에서 삽입 시 루트까지 영향을 미치는 상황

삽입 노드를 **p** 라 할 때, 삼촌이 계속 RED인 경우 색 재칠이 위로 전파된다.

| 노드 | 역할 | 처리 |
|------|------|------|
| **p** | 삽입 (RED) | RED로 삽입 |
| **p¹** | 부모 (RED) | p¹의 부모 = p²가 BLACK인지 확인 |
| **p²** | 조부 (BLACK) | p¹, p²의 형제(삼촌) → BLACK으로 / p² → RED로 |
| **p³** | 증조부 (RED) | p²가 RED가 되어 성질3 위반 재발생 |
| **p⁴** | 고조부 (BLACK) | p³, p⁴의 형제 → BLACK으로 / p⁴ → RED로 |
| **p⁵** | 고고조부 (RED) | p⁴가 RED가 되어 성질3 위반 재발생 |
| **p⁶** | 고고조할머니 (BLACK) | p⁵, p⁶의 형제 → BLACK으로 / p⁶ → RED로 |

이처럼 **삼촌이 RED인 상황(Case 1)이 반복**되면 2레벨씩 위로 전파된다.  
최종적으로 루트에 도달하면 루트를 BLACK으로 칠하며 종료된다.  
최악의 경우 **O(log n)**번 색 재칠이 발생하며 루트까지 영향을 미칠 수 있다.

---

## Q10. B-트리에서 키 18 삽입 (노드당 최대 5개 키)

### 삽입 전 B-트리

```
                  [6  13  25  34  40]
       /       /        |        \        \       \
[1 2 3 4 5] [7 8 9 10] [15 17 19 20 23] [27 28 30 31 33] [35 36 37 38 39] [41 45]
```
### 삽입 후 최종 B-트리

```
                       [25]
              /                   \
       [6  13  19]              [34  40]
     /    |    |   \            /      \
[1-5] [7-10] [15 17 18] [20 23] [27-33] [35-39] [41 45]
```

---

## Q11. 결과 트리에 키 16 삽입

### 삽입 후 최종 B-트리

```
                       [25]
              /                   \
       [6  13  19]              [34  40]
     /    |    |    \            /      \
[1-5] [7-10] [15 16 17 18] [20 23] [27-33] [35-39] [41 45]
```

---

## Q12. B-트리에서 키 22 삭제 (노드당 최대 5개 키)

### 삭제 전 B-트리

```
              [15]
            /      \
         [3 8]    [19 22]
        /  |  \   /   |   \
      [1 2][5 6][9 10][16 18][20 21][24 25 26]
```


### 삭제 후 최종 B-트리

```
              [15]
            /      \
         [3 8]    [19 24]
        /  |  \   /   |   \
      [1 2][5 6][9 10][16 18][20 21][25 26]
```

---

## Q13. 결과 트리에서 키 19 삭제
9
### 삭제 후 최종 B-트리

```
              [15]
            /      \
         [3 8]    [18 24]
        /  |  \   /   |   \
      [1 2][5 6][9 10][16][20 21][25 26]
```

---

## Q14. 내부 노드 원소의 직후 원소는 항상 리프 노드에 있는 이유

B-트리에서 키 k의 **직후 원소(in-order successor)**는 k의 **오른쪽 서브트리**에서 가장 작은 값이다.

오른쪽 서브트리의 최솟값을 찾으려면 해당 서브트리의 루트에서 시작해서 **계속 왼쪽 자식을 따라 내려간다**. 내부 노드는 반드시 자식 포인터를 가지므로, 더 이상 왼쪽 자식이 없는 노드, 즉 **리프 노드**에 도달할 때까지 내려갈 수 있다.

따라서 직후 원소는 항상 리프 노드에 위치한다.

---

## Q15. B-트리 한 노드의 최대 키 수 설계

노드 구조: `[P₀ | K₁ P₁ | K₂ P₂ | ... | Kₙ Pₙ]`

- 포인터 수 = n+1, 키 수 = n
- 부등식: `4(n+1) + 20n ≤ 8192`
- `24n + 4 ≤ 8192`
- `24n ≤ 8188`
- `n ≤ 341.16...`

**→ 최대 341개의 키를 가지도록 설계한다.**

---

## Q16. 리프 노드 키 수와 내부 노드 키 수의 관계

최대 k개의 키를 가지는 B-트리에서:

- 내부 노드의 키는 자식들을 분리하는 **구분자(separator)** 역할
- 리프 노드의 총 수를 L, 내부 노드의 총 수를 I라 하면, **전체 키 수 = 리프의 키 수 + 내부의 키 수**

일반적인 관계:
- **리프 노드의 총 키 수 > 내부 노드의 총 키 수**
- 내부 노드는 탐색 경로를 위한 인덱스만 가지며, 실제 데이터(또는 레코드 포인터)는 리프에 집중됨

특히 B+ 트리에서는 모든 실제 데이터가 리프에만 존재하므로 이 관계가 더욱 명확하다.

---

## Q17. B-트리 병합 시 오버플로우 가능성

**오버플로우는 발생하지 않는다.**

**이유**:  
언더플로우가 발생한 노드의 키 수 ≤ ⌈k/2⌉ - 2 (최솟값 미만)  
형제 노드의 키 수 = ⌈k/2⌉ - 1 (최솟값, 그 이상이면 빌려오는 방식으로 처리됨)

병합 후 총 키 수:
```
(⌈k/2⌉ - 2) + (⌈k/2⌉ - 1) + 1(부모에서 내려온 키) = 2⌈k/2⌉ - 2 ≤ k
```

결과가 최대 키 수 k를 초과하지 않으므로 **오버플로우는 발생하지 않는다**.

---

## Q18. B-트리 높이 범위 (최대 키 수 1,000, 키 1조 개)

1조 = 10¹², 노드당 최대 키 수 = 1,000, 최소 키 수 = ⌈1000/2⌉ - 1 = 499

**최소 높이** (각 노드가 최대 1,001개의 자식):

$$1001^{h-1} \geq 10^{12} \Rightarrow h-1 \geq \log_{1001}(10^{12}) \approx 4 \Rightarrow h_{min} = 5$$

**최대 높이** (각 노드가 최소 500개의 자식, 루트는 최소 2개):

$$2 \times 500^{h-1} \geq 10^{12} \Rightarrow 500^{h-1} \geq 5 \times 10^{11} \Rightarrow h-1 \geq \log_{500}(5 \times 10^{11}) \approx 4.3 \Rightarrow h_{max} = 6$$

**→ B-트리의 높이 범위: 5 ≤ h ≤ 6**

---

## Q19. 디스크 접근 횟수를 줄이는 실용적 아이디어

트리 구조를 변경하지 않고 실용적으로 디스크 접근 횟수를 줄이는 방법:

 **루트 노드 메모리 상주 (Root Caching)**  
루트는 모든 탐색의 시작점이므로 항상 메모리에 올려두면 매 탐색마다 1회의 디스크 접근을 절약할 수 있다.


